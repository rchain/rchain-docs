import React from 'react';
import Layout from '@theme/Layout';

export default function RChainStakingRequest() {
  return (
    <Layout title="RChain staking request" description="Code generation for staking request on RChain">
      <div
        style={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          height: '50vh',
          fontSize: '20px',
        }}>
        <p>
          Hello this is the React comp for staking request
        </p>
      </div>
    </Layout>
  );
}